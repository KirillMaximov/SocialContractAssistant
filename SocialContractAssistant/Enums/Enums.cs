namespace SocialContractAssistant.Enums
{
    public enum QuestionType
    {
        simple = 1,
        start = 2,
        before = 3,
        after = 4,
        finish = 5,
        replace = 6
    }

    public enum PaymentType
    {
        documents = 10,
        consult = 20
    }
}
